Introduction
1.introduce your self and roles responsibilities and frameworked you have worked
2.rate your self in java
3. SQL query joins (self joins) and joins and write the query
4. super keyword
5. framework in details ( POM Framework)
6.Java program Select fullName,ManagerId from tables self Join managerId


               fullName | Manager



               Write a method to return the odd numbers and their frequency from an integer array list passed as an input parameter.

               //I/P: 1, 2, 3, 4, 4, 5, 6, 7, 7, 5

               // O/P: {1=1, 3=1, 5=2, 7=2}
                ArrayList<Integer> arr=new ArrayList();

               public MapArrayList<Integer> method(arr){
               Map <Integer,Integer> map=new HashMap<>();

               for(integer i: arrList){
                if(i % 2 !=0){
               	map.put(i,map.get(i+1));
               	}
               else{map.put(i,1);}

               for(Map.Entry<Integer,Ingeter> entry: map.entrySet)
               {
               	Syso(entry.getKey() +"="+entry.getValue());

               }
               	}
               }

7.